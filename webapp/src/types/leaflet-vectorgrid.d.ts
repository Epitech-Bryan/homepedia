// Type stub for leaflet.vectorgrid — the package side-effect-imports onto
// the global L namespace and ships no .d.ts. We narrow what we touch from
// CityVectorGridLayer; everything else is left as unknown so future use
// stays type-safe.

declare module "leaflet.vectorgrid" {
  // Side-effect import only. Augments the leaflet module at runtime.
}
